n=int(input())

count=0
for i in range(1,n+1):
  if len(str(i))<=2:
    count+=1
  if len(str(i))==3:
    a=str(i)
    x=int(a[0])-int(a[1])
    y=int(a[1])-int(a[2])
    if x==y:
      count+=1

print(count)

문자열로 입력받은걸 int로 변환하고 이를 다시 문자열로 변환한다.

일단 변환하면 마치 리스트처럼 인덱스를 이용하거나 len같은 함수를 이용할 수 있다.
빼기 같은 사칙연산을 할때는 다시 int로 변환하는 것도 잊지 말자
