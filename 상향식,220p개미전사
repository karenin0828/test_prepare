n= int(input())

array= list(map(int, input().split()))

d= [0]* 100

d[0]= array[0]
d[1]= max(array[0], array[1])

for i in range(2,n):
  d[i]= max(d[i-1], d[i-2]+array[i])

print(d[n-1])

1. d[i]를 i번째 창고까지의 최적의 해로 생각한다.

2. 특정번호까지의 최적의 해를 알려면 i-1이랑 i-2에 특정번호를 더한걸 비교해야함.
즉 i-1번째 해와 i-2번째 해를 알아야 한다. 큰문제를 해결하기 위해 작은문제 두개가 필요.
최적부분구조라는 말임

3. 두개만 선택해야 한다는 생각을 버려라. 예시를 통해 넓게봐야지..

4. 바텀업 방식에서 초항 설정하는 과정에 익숙해져야할듯
